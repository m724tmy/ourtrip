<h1>Rooms#index</h1>
<p>Find me in app/views/rooms/index.html.erb</p>

    <div class="new-chat">
      <%= link_to "しおりを作成する", new_room_path %>
    </div>

<div class="rooms">
  <% current_user.rooms.each do |room| %>
    <div class="room">
      <div class="room-name">
        <a href="#"><%= room.room_name %></a>
      </div>
    </div>
  <% end %>
</div>